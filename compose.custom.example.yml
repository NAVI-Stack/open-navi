# Copy to compose.local.yml (gitignored) and merge:
#   docker compose -f compose.yml -f compose.local.yml up -d --build
#
# Examples below are illustrative — uncomment and adapt.

# services:
#   navid:
#     environment:
#       NAVI_ANTHROPIC_KEY: sk-ant-...
#       NAVI_DEBUG: "1"
#     # Optional: mount a custom runtime file
#     # volumes:
#     #   - ./my-runtime.yaml:/navi/config/runtime.yaml:ro
#
# Optional: run Ollama next to NaviD instead of on the host
#   ollama:
#     image: ollama/ollama:latest
#     networks:
#       - default
#   navid:
#     depends_on:
#       - ollama
#     environment:
#       NAVI_OLLAMA_URL: http://ollama:11434/v1
